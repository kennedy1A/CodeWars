//Approach 1 compliting the solution to return true for a string passed
//And The second string (second argument must be exactly what a string ends with)
//Approach we have to check a string ending argument using endwith() method
//This will check if the value in ending is in the string passed. 
// Step one if condition to check if str end with endsWith(ending)
function solution(str, ending){
  if(str.endsWith(ending)){
    return true
  }else{
    return false
  }
  // TODO: complete
}